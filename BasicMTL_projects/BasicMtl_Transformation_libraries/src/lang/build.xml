<!-- $Id: build.xml,v 1.4 2003-08-09 15:19:50 jpthibau Exp $ -->
<project name="BasicMtl_Transformation_libraires" default="Standard_antlr2tll" basedir=".">
	<description>
        Build file for the different BasicMTL files in this project
	</description>
	<!-- set global properties for this build -->
	<property name="src" location="./src"/>
	<property name="bin" location="../../bin/Standard"/>
	<property name="build" location="../../build/Standard"/>

	<target name="init">
    	<!-- Create the time stamp -->
    	<tstamp/>
	    <!-- Create the build directory structure used by compile -->
    	<mkdir dir="${build}"/>
	    <mkdir dir="${build}/src"/>
	    <mkdir dir="${build}/src/UncheckedTLL"/>
	    <mkdir dir="${build}/src/TLL"/>
	    <mkdir dir="${build}/src/java"/>
	    <mkdir dir="${bin}"/>
	</target>
	
	<target name="Standard_antlr2tll" depends="init"
  	 	description="Parse Standard.mtl to a TLL">
	 	<java jar="../../../TLLTypeChecker/dist/bin/TLLTypeChecker.jar" 
           fork="true"
           failonerror="true"
           >
         <arg value="${src}\Standard.mtl"/> 
         <arg value="-TLLPath"/> 
         <arg value="${build}/src/"/> 
         <arg value="-PackageName"/> 
         <arg value="Standard.src.java.Standard"/> 
       </java>
 
	</target>

	<target name="compile Standard" depends="Standard_antlr2tll"
  	 	description="Generate code for Standard.mtl">
	    <mkdir dir="${build}/src/java/Standard"/>
	 	<java jar="../../../BasicMtlJavaCodeGenerator/dist/bin/TLLCodeGenerator.jar" 
           fork="true"
           failonerror="true"
           >
         <arg value="Standard"/> 
         <arg value="-TLLPath"/> 
         <arg value="${build}/src/"/> 
         <arg value="-BinPath"/> 
         <arg value="${build}/src/java/Standard/"/> 
       </java>
 
	</target>

	<target name="clean"
        description="clean up SimpleSamples" >
    	<!-- Delete the ${build}, {$bin} and ${dist} directory trees -->
    	<delete dir="${build}/src/java"/>    
    	<delete dir="${build}"/>    
  	</target>
</project>
